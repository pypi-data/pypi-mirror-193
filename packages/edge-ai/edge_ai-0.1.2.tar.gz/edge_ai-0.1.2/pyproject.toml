[tool.poetry]
name = "edge-ai"
version = "0.1.2"
description = ""
authors = ["Jorge Silva <jorge@edgeimpulse.com>"]
readme = "README.md"
packages = [{include = "edge_ai"}]

[tool.poetry.scripts]
ei = "edge_ai.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.7.0"}
requests = "^2.28.2"
pandas = "^1.5.3"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.1"
typer-cli = "^0.0.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
