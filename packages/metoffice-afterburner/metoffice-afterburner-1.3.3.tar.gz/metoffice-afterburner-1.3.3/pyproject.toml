[build-system]
requires = [
    'setuptools>=42',
    'wheel'
]
build-backend = "setuptools.build_meta"

# Configure coverage.
[tool.coverage.run]
command_line = 'setup.py test -s'

[tool.coverage.report]
fail_under = 75
