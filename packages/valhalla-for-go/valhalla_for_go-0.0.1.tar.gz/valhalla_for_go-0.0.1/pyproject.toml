[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "valhalla_for_go"
version = "0.0.1"
authors = [
  { name="Yu Peijia", email="peijia@nextbillion.ai" },
]

description = "package two functions locate() and decode_polyline()"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
