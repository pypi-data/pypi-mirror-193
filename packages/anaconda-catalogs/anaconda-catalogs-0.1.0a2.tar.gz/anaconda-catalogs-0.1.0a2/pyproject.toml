[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "anaconda-catalogs"
description = "Client library for Anaconda Catalogs"
authors = [
    {name = "Matt Kramer", email = "mkramer@anaconda.com"},
]
license = {text = "BSD-3-Clause"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "aiohttp",
    "intake",
    "requests",
]
dynamic = ["version"]

[tool.setuptools.dynamic]
version = {attr = "anaconda_catalogs.__version__"}

[project.optional-dependencies]
dev = [
    "mypy",
    "pytest",
    "tox",
    "types-requests",
]
publish = [
    "build",
    "twine",
    "wheel",
]

[project.entry-points."intake.drivers"]
anaconda_catalog = "anaconda_catalogs:open_catalog"

[tool.isort]
profile = "black"
#multi_line_output = 3  # Allow multiple imports on a line, but split when too long
force_single_line = true

[tool.distutils.bdist_wheel]
universal = true
