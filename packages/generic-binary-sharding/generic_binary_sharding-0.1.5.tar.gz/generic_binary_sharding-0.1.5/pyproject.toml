[tool.poetry]
name = "generic-binary-sharding"
version = "0.1.5"
description = ""
authors = ["Hamada Gasmallah <hamada@distributive.network>"]
readme = "docs/README.md"
packages = [{include = "generic_binary_sharding"}]

[tool.poetry.dependencies]
python = "^3.9"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.1"
requests = "^2.28.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
serialize-js = "generic_binary_sharding.serialize:cli"
